name: Build Caddy ForwardProxy

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"
  release:
    types:
      - published

permissions:
  contents: write

jobs:
  build:
    name: Build linux-${{ matrix.arch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
          - arch: arm64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: stable

      - name: Install xcaddy
        run: go install github.com/caddyserver/xcaddy/cmd/xcaddy@latest

      - name: Build Caddy
        env:
          GOOS: linux
          GOARCH: ${{ matrix.arch }}
        run: |
          "$(go env GOPATH)/bin/xcaddy" build \
            --output caddy-forwardproxy \
            --with github.com/caddyserver/forwardproxy

      - name: Package
        run: |
          asset="caddy-forwardproxy-linux-${{ matrix.arch }}.tar.gz"
          tar -czf "$asset" caddy-forwardproxy
          sha256sum "$asset" > "$asset.sha256"

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: caddy-forwardproxy-linux-${{ matrix.arch }}
          path: |
            caddy-forwardproxy-linux-${{ matrix.arch }}.tar.gz
            caddy-forwardproxy-linux-${{ matrix.arch }}.tar.gz.sha256

      - name: Upload release assets
        if: github.event_name == 'release' || startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            caddy-forwardproxy-linux-${{ matrix.arch }}.tar.gz
            caddy-forwardproxy-linux-${{ matrix.arch }}.tar.gz.sha256
